pull_request_rules:
  - name: automatically merge approved PRs
    conditions:
      - base = main
      - '#approved-reviews-by >= 1'
      - check-success = @github-actions/result
    actions:
      merge:
        method: squash
