# Generated by hack/update-helm-resources.sh — DO NOT EDIT
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.gateway.name | default "cubestack-gateway" }}
  namespace: {{ .Release.Namespace }}
spec:
  allowedListeners:
    namespaces:
      from: All
  gatewayClassName: {{ .Values.gateway.className | default "eg" }}
  infrastructure:
    parametersRef:
      group: gateway.envoyproxy.io
      kind: EnvoyProxy
      name: {{ .Values.gateway.name | default "cubestack-gateway" }}-proxy
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: http
    port: 80
    protocol: HTTP

